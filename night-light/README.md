# night-light

`night-light` warms the display in the evening and returns it to normal in the morning. It is one setting for the whole session, applied to every screen.

## references

sunrise and sunset come from `libnova`. The warmth values come from `libcolord` (GNOME's night-light reads this).
